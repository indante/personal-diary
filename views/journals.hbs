<h2>{{ result.title }}</h2>
<p>{{ result.content }}</p>
<time datetime="{{ result.created_at }}">Create Time: {{ result.created_at }}</time><br><br>

<form action="/delete/{{ result.id }}" method="POST">
    <a class="btn btn-outline-primary" href="/edit/{{ result.id }}" role="button">수정</a>
    <button class="btn btn-outline-danger" type="submit">삭제</button>
    <a class="btn btn-primary" href="/diaryList" role="button">홈으로</a>
</form>
